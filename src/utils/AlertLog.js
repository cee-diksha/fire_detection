import React from 'react'
/**
 * 
 * @param {number} temperature  temprature as a number 
 * @returns 
 */

const AlertLog = ({temprature}) => {
    if(temprature){
      return ``
    }
}

export default AlertLog
